{% comment %}
Variant Family snippet for Horizon 3.0.1
Uses closest.product to safely access product metafields
Displays a row of sibling products if metafield exists
Shows fallback text if no products are assigned
{% endcomment %}

{% assign current_product = closest.product | default: product %}
{% assign family_products = current_product.metafields.custom.variant_family.value %}

{% if family_products != blank %}

  <div class="variant-family" role="group" aria-label="Choose model">

    {% comment %}
    Single loop: output current product first, then others
    {% endcomment %}
    {% for family_product in family_products %}
      {% if family_product.id == current_product.id %}
        {% assign first_product = family_product %}
      {% endif %}
    {% endfor %}

    {% for product_to_show in family_products %}
      {% assign show_product = product_to_show %}
      {% if product_to_show.id == current_product.id %}
        {% assign show_product = first_product %}
      {% endif %}

      <a href="{{ show_product.url }}" 
         class="variant-family__item {% if show_product.id == current_product.id %}active{% endif %}" 
         aria-current="{% if show_product.id == current_product.id %}true{% else %}false{% endif %}" 
         title="{{ show_product.title }}">
        
        {% if show_product.featured_image %}
          <img src="{{ show_product.featured_image | image_url: '100x100' }}" 
               alt="{{ show_product.title }}" 
               width="50" height="50">
        {% else %}
          <span class="variant-swatch" aria-label="{{ show_product.title }}"></span>
        {% endif %}
      </a>
    {% endfor %}

  </div>

  <style>
    .variant-family {
      display: flex;
      gap: 10px;
      margin-top: 1rem;
      flex-wrap: wrap;
    }

    .variant-family__item {
      display: block;
      border: 2px solid transparent;
      border-radius: 8px;
      padding: 2px;
      transition: border-color 0.2s;
    }

    .variant-family__item.active {
      border-color: #000;
    }

    .variant-family__item image,
    .variant-swatch {
      width: 50px;
      height: 50px;
      object-fit: cover;
      border-radius: 4px;
      display: block;
    }

    .variant-swatch {
      background-color: #ccc;
      display: inline-block;
    }

    .variant-family__item:focus {
      outline: 2px solid #000;
      outline-offset: 2px;
    }

    .variant-family__item:hover {
      border-color: #555;
    }
  </style>

{% else %}
  <div class="variant-family-empty" style="margin-top:1rem; color:#555; font-style:italic;">
    Tuotteita ei määritelty
  </div>
{% endif %}
