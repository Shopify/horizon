{% comment %}
  Aero Storefront Intelligence Engine - Chat Widget
  Loads the self-contained widget from V12 on Railway.
  The widget includes: FAB trigger, chat panel, SSE streaming,
  Seed Doc generation, typing indicators, mobile responsive layout.
  All styling is inline in the JS bundle — no CSS conflicts with theme.

  EXCLUDED on homepage — aero-hero.liquid loads the script directly
  there so the FAB doesn't appear alongside the hero. The hero IS the
  intake surface on the homepage.
  EXCLUDED on password page.
{% endcomment %}

{% if template.name != 'password' and template.name != 'index' %}
<script src="https://multiplist.ai/aero-widget.js" defer></script>
{% endif %}
